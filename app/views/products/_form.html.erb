<%= form_with(model: @product) do |form| %>
  <% if product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<!--  <div class="flex flex-col mb-2">-->
    <%#= form.label :seller_id %>
    <%#= form.text_field :seller_id, class: "border rounded-lg p-2" %>
<!--  </div>-->

  <div class="flex flex-col mb-2">
    <%= form.label :name %>
    <%= form.text_field :name, class: "border rounded-lg p-2" %>
  </div>

  <div class="flex flex-col">
    <%= form.label :description %>
    <%= form.text_area :description, class: "border rounded-lg p-2" %>
  </div>

  <div class="flex flex-col">
    <%= form.label :price %>
    <%= form.text_field :price, class: "border rounded-lg p-2" %>
  </div>

  <%= form.submit :class => "border rounded bg-green-500 text-black p-2 mt-3 hover:brightness-90 transition-all cursor-pointer" %>
<% end %>